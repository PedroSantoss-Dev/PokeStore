import { CartContainer } from "./style"
import { MenuProps } from './interface';


export const  ShoppingCart = ({show}:MenuProps) => {

    return(
        <CartContainer show = {show}>
            
        </CartContainer>
    )
}